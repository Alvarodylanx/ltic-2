{
  "name": "@ltic/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "push": "drizzle-kit push",
    "generate": "drizzle-kit generate",
    "seed": "ts-node --transpile-only src/seed.ts",
    "migrate": "ts-node --transpile-only src/migrate-categories.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "pg": "^8.11.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.0",
    "ts-node": "^10.9.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.4.0"
  }
}
